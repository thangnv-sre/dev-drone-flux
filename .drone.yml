kind: pipeline
type: docker
name: default

steps:
- name: greeting
  image: alpine
  commands:
  - echo hello
  - echo world
  - apk update && apk add wget
  - wget ifconfig.me

trigger:
  event:
  - push